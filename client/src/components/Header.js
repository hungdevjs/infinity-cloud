import React from 'react'

const headerStyle = {
    height: '50px',
    position: 'fixed',
    top: 0,
    left: 0
}

export default () => <div className='w-100 d-flex align-items-center justify-content-center' style={headerStyle}>
    <span>Infinity cloud</span>
</div>